        % coll_hist(n_steps,1) = diagnostics(1);
        % coll_hist(n_steps,2) = diagnostics(2);
        % coll_hist(n_steps,3) = diagnostics(3);
        % coll_hist(n_steps,4) = diagnostics(4);
        % coll_hist(n_steps,5) = diagnostics(5);
        % coll_hist(n_steps,6) = diagnostics(6);
        % coll_hist(n_steps,7) = diagnostics(7);
        
                xHistory(n_steps,p) = particles(p).x;
        yHistory(n_steps,p) = particles(p).y;
        zHistory(n_steps,p) = particles(p).z;